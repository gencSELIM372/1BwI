let a = 70
let b = 85

// || = oder 
// && = und
// >= <= prüfen Bereiche
// parseInt = macht aus texteigaben Zahlen
// == vergleich 
// === streng vergleichen


if (a == 70 || b == 85){
    console.log("ist richtig");Aufgabe 2: Geschwindigkeitswarnung

Beschreibung:

Überprüfen Sie, ob ein Fahrer basierend auf Geschwindigkeit und Verkehr langsamer fahren sollte.

Anweisungen:

Erstellen Sie zwei Variablen:

speed: Zahl (km/h)

trafficLevel: Zahl (1-10)

Verwenden Sie eine if-else-Anweisung mit ||, um zu prüfen:

Die Geschwindigkeit liegt über 80 ODER das Verkehrsaufkommen ist größer als 7.

Geben Sie "Langsamer fahren!" aus, wenn dies zutrifft, andernfalls "Alles in Ordnung."

≥ 30 UND Herzfrequenz zwischen 80 und 120 → "Mäßige Bewegung"
} else {
    console.log("ist falsch");
}